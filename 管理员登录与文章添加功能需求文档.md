# 项目需求文档

**管理员登录与文章添加功能**

版本 v1.2 ｜ 文档日期 2026-09-08 ｜ 项目：个人主页网站后台

## 变更日志

| **版本** | **日期** | **作者** | **变更内容** |
| --- | --- | --- | --- |
| v1.2 | 2026-09-08 | 产品 / 研发 | 新增「文章评论」功能需求（5.5）：评论表单与展示、字段校验、XSS 过滤、防重复提交与按 IP 限流、后台评论管理与删除；权限矩阵、验收标准、性能指标与迭代计划同步更新。 |
| v1.1 | 2026-04-20 | 产品 / 研发 | 新增「删除文章」功能需求（5.4）：文章列表、删除二次确认、删除身份校验与边界条件；权限矩阵、验收标准、迭代计划同步更新。 |
| v1.0 | 2026-04-20 | 产品 / 研发 | 初版发布：定义管理员登录、管理员权限管理、文章添加三大功能的需求范围、字段规则、边界条件与验收标准。 |

## 1 文档说明

### 1.1 文档目的

### 1.2 适用范围与读者

- 适用范围：个人主页网站后台管理系统（仅管理员使用，无普通用户体系）。
- 读者：产品经理、前端/后端开发工程师、测试工程师。

### 1.3 术语与约定

- 管理员：网站所有者，系统的唯一后台使用者。
- 会话（Session）：登录成功后由服务端创建并维护的登录状态，用于后续权限验证。
- bcrypt：单向密码哈希算法，用于密码加密存储。
- 优先级：P0 必须实现 / P1 建议实现 / P2 后续迭代。

## 2 背景与目标

### 2.1 项目背景

| **#** | **问题** | **现状** | **期望** |
| --- | --- | --- | --- |
| P1 | 内容无法在线管理 | 文章维护依赖直接编辑 HTML/JS 文件，效率低且易出错 | 管理员通过后台表单在线发布文章，内容自动入库 |
| P2 | 后台无身份认证 | 管理入口无登录机制，任何人可访问 | 仅凭管理员账户密码可登录后台 |
| P3 | 无权限控制 | 添加文章页面与提交接口未做身份校验 | 仅管理员可访问添加页面，每次提交前校验身份 |
| P4 | 安全隐患 | 密码无加密存储、传输未强制 HTTPS | 密码 bcrypt 加密存储，全站 HTTPS 传输 |
| P5 | 内容无法下线 | 已发布文章无法在后台删除，过期内容只能改代码处理 | 管理员可在后台删除文章，删除后前台立即下架 |

### 2.2 目标与指标

| **编号** | **指标** | **基线** | **目标** | **衡量方式** |
| --- | --- | --- | --- | --- |
| M1 | 正确凭据登录成功率 | 无 | 100%（一次成功） | 登录用例通过率 |
| M2 | 未授权访问拦截率 | 0% | 100% | 未登录访问后台页面/接口全部被拦截 |
| M3 | 文章入库成功率 | 无 | 100% | 校验通过的文章全部成功写入数据库 |
| M4 | 密码加密覆盖率 | 0% | 100% | 数据库抽查，无明文密码 |
| M5 | 非法输入拦截率 | 无 | 100% | 前端+后端双层校验，非法输入全部被拦截 |

## 3 用户与角色

### 3.1 角色定义

| **角色** | **描述** | **核心痛点** | **使用场景** | **技术能力** | **系统权限** |
| --- | --- | --- | --- | --- | --- |
| 管理员 | 网站所有者，后台唯一使用者 | 无法在线发布内容、担忧数据与账号安全 | 登录后台、发布文章、管理内容 | 基础操作能力 | 后台全部功能 |
| 访客（匿名） | 前台浏览用户，非系统注册用户 | 无 | 浏览网站前台文章 | 无 | 仅前台阅读，无任何后台权限 |

### 3.2 权限矩阵

| **操作** | **管理员** | **访客（匿名）** |
| --- | --- | --- |
| 登录后台 | ✓ | ✗ |
| 访问文章添加页面 | ✓ | ✗ |
| 提交/发布文章 | ✓ | ✗ |
| 删除文章 | ✓ | ✗ |
| 浏览前台文章 | ✓ | ✓ |
| 发表评论 | ✓ | ✓ |
| 查看评论 | ✓ | ✓ |
| 删除评论 | ✓ | ✗ |

## 4 全局前置条件

| **类型** | **项目** | **说明** |
| --- | --- | --- |
| 环境 | HTTPS | 生产环境强制 HTTPS（HSTS），杜绝明文传输 |
| 环境 | 数据库 | 管理员账户表与文章表已初始化，含默认管理员账户 |
| 环境 | 依赖 | 服务端支持 Session 与会话管理，具备 bcrypt 密码哈希能力（如 Node.js + express-session + bcryptjs） |
| 账号 | 管理员账户 | 数据库中管理员密码以 bcrypt 哈希存储，不使用明文 |
| 安全 | 参数化查询 | 所有 SQL 均使用参数化查询，防止 SQL 注入 |

## 5 功能需求详述

### 5.1 管理员登录（LOGIN）

#### 5.1.1 前置条件

- 系统已部署 HTTPS；管理员账户已在数据库中创建（密码为 bcrypt 哈希）。

#### 5.1.2 功能需求

| **编号** | **优先级** | **功能项** | **详细描述 / 字段规则 / 边界条件** |
| --- | --- | --- | --- |
| REQ-01 | P0 | 登录页面 | 提供「用户名」「密码」输入框与登录按钮；支持回车提交；输入框带输入提示（placeholder）。 |
| REQ-02 | P0 | 防空白提交 | 前端校验：用户名、密码 trim 去空白后非空才允许提交；后端同样校验，任一为空返回错误提示「请输入用户名和密码」。 |
| REQ-03 | P0 | 登录验证 | 校验用户名与密码是否与数据库管理员账户一致；密码比对使用 bcrypt；用户名不存在与密码错误返回统一提示「用户名或密码错误」，防止账号枚举。 |
| REQ-04 | P0 | 会话管理 | 登录成功后创建服务端 Session，记录会话变量（管理员 ID、登录时间、过期时间），默认有效期 30 分钟并滑动续期；用于后续权限验证。 |
| REQ-05 | P0 | 退出登录 | 后台提供退出入口，退出时销毁会话并跳转登录页。 |
| REQ-06 | P0 | 传输安全 | 全站 HTTPS；登录凭据仅通过 POST 请求体传输，禁止在 URL 中携带用户名/密码。 |
| REQ-07 | P0 | 密码加密存储 | 管理员密码以 bcrypt（cost ≥ 10）哈希存储，禁止明文或可逆加密存储。 |
| REQ-08 | P1 | 防暴力破解 | 同一账户连续 5 次登录失败后锁定 15 分钟，并记录失败日志；锁定期间拒绝登录。 |

#### 5.1.3 边界条件

| **编号** | **边界场景** | **触发条件** | **系统行为** | **恢复策略** |
| --- | --- | --- | --- | --- |
| BC-01 | 空白提交 | 用户名或密码为空 / 全为空白字符 | 前端阻止提交并提示；后端再次校验返回 400 | 补全后重试 |
| BC-02 | 用户名不存在 | 提交未注册用户名 | 统一提示「用户名或密码错误」，不区分原因 | 核对凭据后重试，失败计数+1 |
| BC-03 | 密码错误 | 密码与 bcrypt 哈希不匹配 | 统一提示「用户名或密码错误」 | 核对凭据后重试，失败计数+1 |
| BC-04 | 账户被锁定 | 连续 5 次失败触发锁定 | 提示「账户已锁定，请 15 分钟后再试」 | 锁定到期自动解锁 |
| BC-05 | 会话过期/无效 | Session 过期或被销毁 | 后台操作被拒并跳转登录页 | 重新登录 |
| BC-06 | 传输降级 | 请求通过 HTTP 发起 | 强制跳转 HTTPS（HSTS） | 自动重定向，无需用户操作 |
| BC-07 | 数据库不可用 | 管理员表查询失败 | 提示「系统繁忙，请稍后重试」，不泄露内部错误 | 服务恢复后重试 |

### 5.2 管理员权限管理（AUTH）

#### 5.2.1 功能需求

| **编号** | **优先级** | **功能项** | **详细描述 / 字段规则 / 边界条件** |
| --- | --- | --- | --- |
| REQ-09 | P0 | 页面访问控制 | 仅管理员可访问「添加文章」页面；未登录访问时重定向到登录页，登录成功后回跳原页面；无权限请求返回 403。 |
| REQ-10 | P0 | 提交身份校验 | 每次提交文章前，服务端必须校验 Session 中管理员身份是否有效；无效则拒绝（401/403）并引导重新登录，确保只有管理员可以提交。 |
| REQ-11 | P1 | 会话时效校验 | 每次提交请求校验会话是否过期，过期则强制重新登录后再次提交。 |
| REQ-12 | P1 | 接口级防护 | 后台写接口（提交文章）校验 CSRF Token，防止跨站请求伪造。 |

#### 5.2.2 边界条件

| **编号** | **边界场景** | **触发条件** | **系统行为** | **恢复策略** |
| --- | --- | --- | --- | --- |
| BC-08 | 未登录直接访问添加页 | 访问 /admin/article/add | 重定向登录页；登录后回跳原页面 | 完成登录 |
| BC-09 | 会话过期后提交 | 停留超 30 分钟后提交 | 拒绝提交并提示重新登录；前端保留已填内容不丢失 | 重新登录后继续编辑 |
| BC-10 | 非管理员请求后台接口 | 无有效会话调用提交接口 | 返回 403，记录访问日志 | 无（需先登录） |
| BC-11 | CSRF Token 缺失/无效 | 提交请求未携带或携带错误 Token | 拒绝请求（403） | 刷新页面重新获取 Token |

### 5.3 文章添加（ARTICLE）

#### 5.3.1 功能需求

| **编号** | **优先级** | **功能项** | **详细描述 / 字段规则 / 边界条件** |
| --- | --- | --- | --- |
| REQ-13 | P0 | 文章表单 | 提供「标题」「内容」「类别」「标签」字段与提交按钮；标题、内容、类别必填，标签选填。 |
| REQ-14 | P0 | 防空白提交 | 前端 + 后端双重校验：所有字段 trim 后按必填规则校验，空值/全空白不允许提交并给出明确提示。 |
| REQ-15 | P0 | 字段校验 | 按 5.3.2 字段规格执行格式校验与最大长度限制，超限或格式非法时拒绝提交并提示。 |
| REQ-16 | P0 | 提交处理 | 提交前再次校验管理员身份（REQ-10）；校验通过后写入文章表；成功后提示「发布成功」并跳转/清空。 |
| REQ-17 | P0 | 内容安全 | 文章内容经 XSS 白名单过滤（去除 script、事件属性等）后存储；前台渲染时进行转义。 |
| REQ-18 | P1 | 防重复提交 | 提交期间按钮禁用防连点；服务端对同一会话短时间内的相同内容去重（5 秒内拒绝第二次）。 |

#### 5.3.2 字段规格

| **字段** | **必填** | **校验规则（格式）** | **最大长度** |
| --- | --- | --- | --- |
| 标题 | 是 | trim 后非空；去除首尾空白；禁止全空白/纯符号 | ≤ 100 字符 |
| 内容 | 是 | trim 后非空；富文本按白名单过滤（去除 script、事件属性、iframe 等）；仅含空白或空标签视为空 | ≤ 50,000 字符 |
| 类别 | 是 | 必须为系统预置类别之一（下拉选择，禁止自由输入） | ≤ 20 字符 |
| 标签 | 否 | 逗号分隔；逐项去空白、去重；仅允许中文/英文/数字/下划线/连字符 | 每项 ≤ 20 字符，最多 5 项 |

#### 5.3.3 边界条件

| **编号** | **边界场景** | **触发条件** | **系统行为** | **恢复策略** |
| --- | --- | --- | --- | --- |
| BC-12 | 标题为空/全空白 | 标题未填或仅空白字符 | 拒绝提交，提示「标题不能为空」 | 填写标题后重试 |
| BC-13 | 标题超长 | 标题超过 100 字符 | 拒绝提交并提示超长；输入框限制最大长度 | 精简标题后重试 |
| BC-14 | 内容为空 | 内容为空、仅空白或仅空标签 | 拒绝提交，提示「内容不能为空」 | 填写内容后重试 |
| BC-15 | 内容超长 | 内容超过 50,000 字符 | 拒绝提交并提示内容过长 | 精简内容后重试 |
| BC-16 | 类别非法 | 提交非预置类别 | 拒绝提交，提示选择有效类别 | 重新选择类别 |
| BC-17 | 标签违规 | 标签超过 5 项或单项超 20 字符/含非法字符 | 拒绝提交并提示标签规则 | 修正标签后重试 |
| BC-18 | XSS 载荷 | 内容含 script 等危险代码 | 白名单过滤后入库；过滤后为空则拒绝 | 无（自动处理） |
| BC-19 | 快速重复提交 | 双击提交按钮或重复请求 | 第二次请求被拦截，仅入库一次 | 无（自动处理） |
| BC-20 | 数据库写入失败 | 文章表写入异常 | 提示「发布失败，请稍后重试」，保留表单内容 | 服务恢复后重试 |

### 5.4 删除文章（ARTICLE-DELETE）

#### 5.4.1 功能需求

| **编号** | **优先级** | **功能项** | **详细描述 / 字段规则 / 边界条件** |
| --- | --- | --- | --- |
| REQ-19 | P0 | 文章列表 | 后台提供文章列表页，展示标题、类别、发布时间等字段，作为删除操作入口；列表支持按标题/类别检索。 |
| REQ-20 | P0 | 删除二次确认 | 每条文章提供「删除」操作；点击后弹出确认对话框并展示文章标题，二次确认后才执行删除，防止误删。 |
| REQ-21 | P0 | 删除身份校验 | 执行删除前，服务端必须校验 Session 中管理员身份是否有效（复用 REQ-10 校验逻辑）；无效则拒绝（401/403）并引导重新登录，确保只有管理员可以删除。 |
| REQ-22 | P0 | 删除处理 | 校验通过后删除数据库中对应文章记录；成功后提示「删除成功」，前台该文章立即下架，不再展示。 |
| REQ-23 | P1 | 批量删除 | 支持在列表页多选文章后批量删除；批量删除同样需二次确认与逐条权限校验。 |
| REQ-24 | P1 | 接口级防护 | 删除接口为写接口，校验 CSRF Token（复用 REQ-12 机制），防止跨站伪造删除请求。 |

#### 5.4.2 边界条件

| **编号** | **边界场景** | **触发条件** | **系统行为** | **恢复策略** |
| --- | --- | --- | --- | --- |
| BC-21 | 未登录访问列表/删除接口 | 未登录访问文章列表页或调用删除接口 | 重定向登录页；接口返回 401/403 并记录日志 | 完成登录后重试 |
| BC-22 | 会话过期后删除 | 停留超 30 分钟后执行删除 | 拒绝删除并提示重新登录 | 重新登录后再次删除 |
| BC-23 | 文章不存在 | 删除已被删除或 ID 非法的文章 | 提示「文章不存在或已被删除」，不中断流程 | 刷新列表 |
| BC-24 | 确认框取消 | 点击删除后选择「取消」 | 不执行任何删除操作，停留在当前页面 | 无 |
| BC-25 | 快速重复删除 | 重复点击删除/确认或重复请求 | 第二次请求被拦截，仅删除一次 | 无（自动处理） |
| BC-26 | 数据库删除失败 | 文章表删除异常 | 提示「删除失败，请稍后重试」，数据保持不变 | 服务恢复后重试 |

## 5.5 文章评论（COMMENT）

### 5.5.1 功能需求

| **编号** | **优先级** | **功能项** | **详细描述 / 字段规则 / 边界条件** |
| --- | --- | --- | --- |
| REQ-25 | P0 | 评论表单 | 文章详情页（article.html）正文下方提供评论区：「昵称」（必填，≤ 20 字符）「邮箱」（选填，≤ 50 字符）「评论内容」（必填，≤ 1000 字符）与提交按钮；提交期间按钮禁用防连点。 |
| REQ-26 | P0 | 字段校验 | 前端 + 后端双重校验：昵称 trim 后非空，否则提示「昵称不能为空」；评论内容去除 HTML 标签后非空（仅空白/纯标签视为空），否则提示「评论内容不能为空」；邮箱填写时必须符合邮件格式，否则提示「邮箱格式不正确」。 |
| REQ-27 | P0 | 评论展示 | 文章详情页展示该文章的评论列表（按提交时间升序、新评论在下方），逐条显示昵称、提交时间与内容，并在评论区标题展示评论总数；无评论时提示「暂无评论，快来抢沙发」。 |
| REQ-28 | P0 | 内容安全 | 评论内容提交后经 XSS 白名单过滤（去除全部 HTML 标签与危险元素，以纯文本入库）；前台渲染时再次转义，双层防御防脚本注入。 |
| REQ-29 | P0 | 文章关联 | 评论只能针对已存在的文章提交；文章不存在或已被删除时返回 404「文章不存在或已被删除」。文章删除后其评论级联删除，不产生孤儿记录。 |
| REQ-30 | P1 | 防重复提交 | 同一 IP 对同一文章提交相同内容，60 秒内第二次起拒绝（429），提示「请勿重复提交评论」。 |
| REQ-31 | P1 | 按 IP 限流 | 同一 IP 每小时最多提交 10 条评论，超出返回 429，提示「评论过于频繁，请稍后再试」。 |
| REQ-32 | P0 | 后台评论管理 | 后台新增「评论管理」Tab：评论列表展示 ID、文章标题、昵称、邮箱、内容、IP、时间，支持按文章 ID 检索；每条提供删除操作。 |
| REQ-33 | P0 | 删除身份校验 | 后台删除评论复用「管理员会话 + CSRF Token」校验（复用 REQ-21 / REQ-24 逻辑）；未登录或 Token 无效返回 401/403 并记录日志。 |

### 5.5.2 字段规格

| **字段** | **必填** | **校验规则（格式）** | **最大长度** |
| --- | --- | --- | --- |
| 昵称 | 是 | trim 后非空；仅去除首尾空白 | ≤ 20 字符 |
| 邮箱 | 否 | 选填；填写时须符合标准邮件格式（xxx@yyy.zz） | ≤ 50 字符 |
| 评论内容 | 是 | 去除全部 HTML 标签后 trim 非空（仅空白/纯标签视为空） | ≤ 1000 字符 |
| 目标文章 | 是（系统自动携带） | 必须为已存在的文章 ID；文章被删除后拒绝评论并级联清理 | — |

### 5.5.3 边界条件

| **编号** | **边界场景** | **触发条件** | **系统行为** | **恢复策略** |
| --- | --- | --- | --- | --- |
| BC-27 | 昵称为空 | 昵称未填或全为空白字符 | 拒绝提交，提示「昵称不能为空」 | 填写昵称后重试 |
| BC-28 | 评论内容为空 | 内容为空、仅空白或仅 HTML 标签 | 拒绝提交，提示「评论内容不能为空」 | 填写内容后重试 |
| BC-29 | 内容超长 | 评论内容超过 1000 字符 | 拒绝提交并提示内容过长 | 精简内容后重试 |
| BC-30 | 邮箱格式非法 | 填写的邮箱不符合邮件格式 | 拒绝提交，提示「邮箱格式不正确」 | 修正邮箱后重试 |
| BC-31 | XSS 载荷 | 评论内容含 script 等危险代码 | 全部标签去除后以纯文本入库，前台转义渲染 | 无（自动处理） |
| BC-32 | 文章不存在 | 向已删除或不存在的文章提交/读取评论 | 返回 404「文章不存在或已被删除」 | 无 |
| BC-33 | 重复评论 | 同 IP + 同文章 + 同内容 60 秒内再次提交 | 返回 429「请勿重复提交评论」 | 无（自动处理） |
| BC-34 | 限流触发 | 同 IP 单小时提交超过 10 条 | 返回 429「评论过于频繁，请稍后再试」 | 1 小时后重试 |
| BC-35 | 未授权删除评论 | 未登录或 CSRF Token 无效时调用后台删除评论接口 | 返回 401/403 并记录日志 | 重新登录后重试 |
| BC-36 | 数据库写入失败 | 评论表写入异常 | 提示「评论发布失败，请稍后重试」 | 服务恢复后重试 |

### 5.5.4 实现思路

- 数据模型：新增 comments 表（id / article_id / nickname / email / content / ip / created_at / created_ms），created_ms 存 UTC 纪元秒，作为 60 秒防重复与每小时限流的时间窗口依据（不受部署时区影响）；article_id 外键引用 articles(id) 并 ON DELETE CASCADE，另建 article_id 索引；Express 本地版由 db.js 启动时幂等建表，Cloudflare D1 版由 ensureSchema 幂等初始化，两端表结构一致。
- 接口设计：GET /api/articles/:id/comments（公开只读，文章不存在返回 404）；POST /api/articles/:id/comments（公开写入，按「文章存在校验 → 字段校验 → XSS 过滤 → 60 秒防重复 → 每小时限流 → 记录客户端 IP 入库」顺序执行）；GET /api/admin/comments?articleId=（需管理员会话，关联文章标题）；DELETE /api/admin/comments/:id（需管理员会话 + CSRF，复用 requireAdminWrite 中间件）。
- 前台交互：article.html 正文下方新增评论区块（评论列表 + 昵称/邮箱/内容表单）；渲染前对评论内容做二次转义（防 XSS 第二道防线，与文章正文一致）；提交期间禁用按钮；400/429/404 时展示服务端提示并保留已输入内容。
- 后台交互：admin/dashboard.html 新增「评论管理」Tab：评论列表支持按文章 ID 检索，删除复用现有二次确认弹窗与 CSRF 请求头（401 自动跳登录页）。
- 安全与双端同步：SQL 全部参数化查询；评论内容以纯文本入库、前端转义渲染；防重复与限流基于客户端 IP 查库实现，重启后仍然有效；本地 Express 版（db.js / server.js）与 Cloudflare Pages Functions 版（functions/_lib.mjs、functions/api/[[path]].mjs）接口与校验规则保持逐条一致，D1 迁移脚本（init-d1.sql 等）同步新增 comments 表。

## 6 非功能需求

### 6.1 性能

- 登录接口响应 P95 ≤ 500ms（含 bcrypt 校验）。
- 评论提交接口响应 P95 ≤ 500ms（含校验、过滤与入库）。
- 文章提交接口响应 P95 ≤ 1s（含内容过滤与入库）。
- bcrypt 单次校验耗时 ≤ 300ms（cost = 10）。

### 6.2 安全

- 全站 HTTPS；密码 bcrypt 加密存储；登录失败限流与账户锁定。
- SQL 参数化查询防注入；内容 XSS 过滤；后台写接口 CSRF Token 防护。
- Session ID 使用 HttpOnly Cookie；会话默认 30 分钟过期。

### 6.3 兼容性

- 支持最新版 Chrome / Edge / Firefox / Safari。
- 后台页面自适应桌面端与移动端。

### 6.4 可维护性

- 后台代码与前台展示分离；字段校验规则集中配置，便于调整长度与格式限制。

## 7 验收标准（DoD）

| **编号** | **验收项** | **预期结果** | **测试方法** | **优先级** |
| --- | --- | --- | --- | --- |
| AC-01 | 登录页防空白提交 | 用户名或密码为空/全空白时，前端拦截且后端返回 400 | 手工 + 接口测试 | P0 |
| AC-02 | 正确凭据登录 | 正确用户名+密码登录成功，跳转后台并建立会话 | 手工测试 | P0 |
| AC-03 | 错误凭据提示 | 密码错误/用户不存在均返回统一提示，不泄露账号存在性 | 手工测试 | P0 |
| AC-04 | 未授权访问拦截 | 未登录访问添加文章页重定向登录；调用提交接口返回 401/403 | 手工 + 接口测试 | P0 |
| AC-05 | 会话过期拦截 | 会话过期后提交被拒绝并提示重新登录，已填内容不丢失 | 手工测试 | P0 |
| AC-06 | 文章入库 | 合法文章提交后写入数据库，页面提示发布成功 | 手工 + 数据核对 | P0 |
| AC-07 | 密码加密存储 | 数据库中管理员密码全部为 bcrypt 哈希，抽查无明文 | 数据库抽查 | P0 |
| AC-08 | 字段校验拦截 | 超长标题、非法类别、超量标签等均被拒绝并给出明确提示 | 边界用例测试 | P1 |
| AC-09 | 暴力破解防护 | 连续 5 次失败后账户锁定 15 分钟 | 自动化/手工测试 | P1 |
| AC-10 | 重复提交防护 | 快速双击仅成功发布一次 | 手工测试 | P1 |
| AC-11 | 删除权限校验 | 未登录调用删除接口返回 401/403，无有效会话无法删除任何文章 | 手工 + 接口测试 | P0 |
| AC-12 | 删除二次确认 | 点击删除弹出确认框并展示文章标题；选择取消不删除 | 手工测试 | P0 |
| AC-13 | 删除生效 | 确认删除后文章从数据库移除，前台不再展示 | 手工 + 数据核对 | P0 |
| AC-14 | 文章不存在处理 | 删除不存在的文章给出「文章不存在或已被删除」提示且不报错 | 边界用例测试 | P1 |
| AC-15 | 评论提交入库 | 合法评论提交后写入 comments 表，前台评论列表立即可见并计入评论数 | 手工 + 数据核对 | P0 |
| AC-16 | 评论校验拦截 | 昵称为空、内容为空/超长、邮箱格式非法均被拒绝并给出明确提示 | 边界用例测试 | P0 |
| AC-17 | 评论 XSS 过滤 | 含 script 等载荷的评论入库后为纯文本，前台渲染无可执行脚本 | 边界用例测试 | P0 |
| AC-18 | 评论删除权限校验 | 未登录或无有效会话调用后台删除评论接口返回 401/403 | 手工 + 接口测试 | P0 |
| AC-19 | 评论防重与限流 | 同 IP 60 秒内重复提交相同评论、单小时超过 10 条均返回 429 并提示 | 自动化测试 | P1 |

## 8 迭代计划

### 阶段一（P0 核心）

- 登录页面与防空白提交、bcrypt 密码校验、会话管理（REQ-01 ~ 07）。
- 页面访问控制与提交身份校验（REQ-09 ~ 11）。
- 文章表单、字段校验、提交入库、XSS 过滤（REQ-13 ~ 17）。

### 阶段二（P1 加固）

- 登录失败锁定、CSRF Token、防重复提交、操作日志（REQ-08 / 12 / 18）。

### 阶段三（文章管理完善）

- 文章列表与删除文章（含二次确认、批量删除）（REQ-19 ~ 24）。

### 阶段四（P2 扩展）

- 文章编辑、草稿保存、富文本编辑器升级。

### 阶段五（评论互动）

- 文章评论功能：评论表单与展示、字段校验、XSS 过滤、防重复提交与按 IP 限流、后台评论管理与删除（REQ-25 ~ 33）。

## 9 风险评估

| **风险项** | **描述与应对** | **影响等级** |
| --- | --- | --- |
| 会话安全 | Session ID 泄露/固定可导致会话劫持；应对：随机 Session ID、HttpOnly Cookie、登录成功轮换 Session ID | 低 |
| 暴力破解 | 密码被批量尝试；应对：失败计数 + 账户锁定 + 登录日志 | 低 |
| XSS 注入 | 文章内容携带恶意脚本影响前台访客；应对：白名单过滤 + 渲染转义，作为 P0 交付 | 中 |
| 数据丢失 | 入库失败或误操作导致文章丢失；应对：写入前校验、数据库定期备份 | 低 |
| 扩展性 | 单管理员模型后续需扩展多角色；应对：管理员表预留角色字段，权限判断集中管理 | 低 |
| 评论刷屏 | 访客通过脚本批量提交评论干扰网站；应对：按 IP 限流（10 条/小时）+ 60 秒防重复 + 后台可检索并删除任意评论 | 低 |

## 10 附录

### 10.1 术语表

- 管理员：网站后台的唯一授权使用者。
- 会话（Session）：服务端维护的登录状态，用于权限验证。
- bcrypt：单向密码哈希算法，不可逆，用于密码安全存储。
- CSRF：跨站请求伪造，通过 Token 校验防护。
- XSS：跨站脚本攻击，通过输入过滤与输出转义防护。

### 10.2 参考资料

- 业务方提供的需求要点《管理员登录与文章添加功能实现》（2026-04-20）。

### 10.3 待决事项

1. 内容字段是否支持富文本编辑器及其类型（纯文本 / Markdown / 富文本）。
2. 「标签」是否必须填写（当前按选填处理）。
3. 登录失败锁定阈值与锁定时长（当前 5 次 / 15 分钟）的最终确认。
4. 删除采用物理删除还是逻辑删除（软删除 + 回收站恢复）。
5. 是否启用草稿功能与文章编辑功能（纳入后续迭代）。

— 文档结束 —
